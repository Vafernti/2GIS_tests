## Проверка ручки POST /v1/auth/tokens

1.1 **Позитивные кейсы**

1.1.1 Получение сессионного токена  
1.1.2 Проверка полученного токена, в рамках времени жизни (2000 мс)  

1.2 **Негативные кейсы**

1.2.1 При отсутствии токена - запрос запрещен  
1.2.2 При устаревшем токене ( > 2000 мс) - запрос запрещен  

## Проверка ручки POST /v1/favorites

2.1 **Позитивные кейсы**

2.1.1 Успешное создание любимого места с валидными данными  
2.1.2 Успешное создание любимого места с валидными данными без поля color  
2.1.3 Отправка запроса с полем title в виде строки на кириллице  
2.1.4 Отправка запроса с полем title в виде строки латинице  
2.1.5 Отправка запроса с полем title в виде строки на латинице с цифрами  
2.1.6 Отправка запроса с полем title в виде строки спецсимволов  
2.1.7 Отправка запроса с полем title в виде строки длиной 1 символ  
2.1.8 Отправка запроса с полем title в виде строки длиной 999 символов  
2.1.9 Отправка запроса с полем lat в виде цифры (int)  
2.1.10 Отправка запроса с полем lat в виде цифры с плавающей точкой (float)  
2.1.11 Отправка запроса с полем lat в виде цифры -90  
2.1.12 Отправка запроса с полем lat в виде цифры 90  
2.1.13 Отправка запроса с полем lon в виде цифры (int)  
2.1.14 Отправка запроса с полем lon в виде цифры с плавающей точкой (float)  
2.1.15 Отправка запроса с полем lon  в виде цифры -180  
2.1.16 Отправка запроса с полем lon в виде цифры 180  
2.1.17 Отправка запроса с полями lat lon с пограничнымыми значениями (попарное тестирование)  
2.1.18 Отправка запросов с полем color в виде строки со значениями (BLUE, RED, GREEN, YELLOW)  

2.2 **Негативные кейсы**

2.2.1 Отправка запроса с полем title со значением None  
2.2.2 Отправка запроса с полем title без значений  
2.2.3 Отправка запроса с полем title только с одним пробелом  
2.2.4 Отправка запроса с полем title в виде строки длиной 1000 символов  
2.2.5 Отправка запроса с полем title со значением в виде цифры (int)  
2.2.6 Отправка запроса с полем lat со значением None  
2.2.7 Отправка запроса с полем lat без значений  
2.2.8 Отправка запроса с полем lat содержащий только один пробелом  
2.2.9 Отправка запроса с полем lat в виде числовой строки  
2.2.10 Отправка запроса с полем lat в виде строки  
2.2.11 Отправка запроса с полем lat со значением -90.0001  
2.2.12 Отправка запроса с полем lat со значением 90.0001  
2.2.13 Отправка запроса с полем lon со значением None  
2.2.14 Отправка запроса с полем lon без значений  
2.2.15 Отправка запроса с полем lon только с одним пробелом  
2.2.16 Отправка запроса с полем lon в виде числовой строки  
2.2.17 Отправка запроса с полем lon в виде строки  
2.2.18 Отправка запроса с полем lon со значением -90.0001  
2.2.19 Отправка запроса с полем lon со значением 90.0001  
2.2.20 Отправка запроса с полем color в виде строки в нижнем регистре  
2.2.21 Отправка запроса с полем color в виде строки в смешанном регистре  
2.2.22 Отправка запроса с полем color с неизвестным значением (прим.: WHITE)  
2.2.23 Отправка запроса с полем color содержащий только один пробел  
2.2.24 Отправка запроса с полем color содержаний пробел до валидных значений  
2.2.25 Отправка запроса с полем color содержаний пробел после валидных значений  
2.2.25 Отправка запроса с полем color содержаний 256 символа  
